# 📚 MyLibrary - Библиотечная система на FastAPI

## ✨ Возможности

- 📖 **CRUD операции** с книгами (добавление, чтение, обновление, удаление)
- 🔍 **Поиск книг** по названию, автору, году издания
- 📊 **Пагинация** для удобной навигации по большим коллекциям
- ✅ **Валидация данных** с помощью Pydantic
- 🗄️ **Автоматическая документация** API (Swagger UI и ReDoc)
- 🔒 **Готово к расширению** (авторизация, категории, пользователи)

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11 или выше
- pip (менеджер пакетов Python)

# 1. Клонируй репозиторий
git clone https://github.com/kostIT13/MyLibrary.git
cd MyLibrary

# 2. Создай виртуальное окружение и активируй
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# 3. Установи зависимости
pip install -r requirements.txt

# 4. Запусти сервер
uvicorn main:app --reload

# 5. Открой в браузере:
# Документация API: http://localhost:8000/docs
# Интерфейс ReDoc: http://localhost:8000/redoc

📚 API Endpoints
Книги
Метод	Endpoint	Описание
GET	/books/	Получить список всех книг с пагинацией
GET	/books/{id}	Получить книгу по ID
POST	/books/	Добавить новую книгу
PUT	/books/{id}	Обновить информацию о книге
DELETE	/books/{id}	Удалить книгу

📁 Структура проекта
text
MyLibrary/
├── main.py              # Точка входа, настройка FastAPI приложения
├── database.py          # Настройка подключения к базе данных
├── repository.py        # Паттерн Repository для работы с данными
├── requirements.txt     # Зависимости проекта
├── .gitignore          # Игнорируемые файлы Git
├── models/             # SQLAlchemy модели
│   ├── __init__.py
│   └── book.py         # Модель Book
├── schemas/            # Pydantic схемы
│   ├── __init__.py
│   └── book.py         # Схемы BookCreate, BookUpdate, BookResponse
└── routers/            # Маршруты API
    ├── __init__.py
    └── books.py        # Роутер для работы с книгами


📖 Документация API
После запуска сервера доступна автоматическая документация:

Swagger UI: http://localhost:8000/docs

ReDoc: http://localhost:8000/redoc

🛠 Технологии
FastAPI - современный веб-фреймворк для создания API

SQLAlchemy - ORM для работы с базой данных

Pydantic - валидация данных и сериализация

Uvicorn - ASGI-сервер для запуска приложения

SQLite - база данных (легковесная, не требует установки)
